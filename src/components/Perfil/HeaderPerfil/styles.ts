import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  header: {
    width: "100%",
    display: "flex",
    flexDirection: "row",
    justifyContent: "space-between",
    alignItems: "center",
    padding: 15,

    // ----Hard code!!!!! ajustar isso urgentemente-----
    marginBottom: 100,
    // -------------------------------------------------
  },
});
export default styles;
